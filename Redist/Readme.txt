M-Files SDK
===========

Installation instructions:

1. Install the M-Files SDK MSBuild tasks by installing the
   MFiles.SDK.Setup.msi
2. Install the M-Files SDK Visual Studio extension by running the
   MFiles.SDK.VisualStudio.Application.vsix

Note: Currently the MFiles.SDK.Setup.msi might not give any notification
      for a successful install.






